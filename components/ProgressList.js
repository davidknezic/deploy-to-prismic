import React from 'react'

const ProgressList = ({
  children,
}) => (
  <div className="progress-list">
    {children}
    <style jsx>{`
      .progress-list {
      }
    `}</style>
  </div>
)

export default ProgressList
